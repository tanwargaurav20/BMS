server:
  port: 9191
spring:
  datasource:
    url: jdbc:h2:mem:mytestdb
    username: sa
    password:
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
jwt:
  secret: token-secret
  expirationDateInMs: 180000